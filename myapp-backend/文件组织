project_name/
│
├── app/                  # 核心应用目录
│   ├── __init__.py
│   ├── main.py           # 入口文件：创建 FastAPI 实例、注册路由
│   ├── routes.py         # 路由定义：所有接口端点集中在这里
│   ├── schemas.py        # Pydantic 模型：请求/响应数据校验
│   ├── db.py             # 数据库相关：连接、简单模型或操作（若用数据库）
│   └── utils.py          # 工具函数：通用逻辑（如格式化、验证等）
│
├── tests/                # 测试目录（简单项目可省略，或只保留单文件）
│   └── test_api.py
│
├── .env                  # 环境变量（如数据库地址、端口等）
├── requirements.txt      # 依赖列表
└── README.md             # 项目说明